다음은 어제 사용했던 text() 실습 05Text.html 입니다
text() / html()은 get/set 두가지 기능 보ㅇㅠ
보유
text() / html() 은 추출 즉, get 방식
text("~~") / html("~~") 은 설정 즉, set 방식
text("~~")  은 내부에 태그가 존재하더라도 문자로 인식

html("~~") 은 내부에 태그가 존재하면 컴파일 됨..